pub mod derivation_path;
pub mod key_derivation;
pub mod keystore;
pub mod pbkdf;
pub mod scrypt;

pub fn add(left: u64, right: u64) -> u64 {
    left + right
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn it_works() {
        let result = add(2, 2);
        assert_eq!(result, 4);
    }
}
